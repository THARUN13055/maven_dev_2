apiVersion: v1
kind: Service
metadata:
  name: my-springboot-service
  namespace: devopsproject
  labels:
    app: myspringbootapp
spec:
  selector:
    app: myspringbootapp
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
  type: LoadBalancer
  